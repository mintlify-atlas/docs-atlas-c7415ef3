---
title: "CLI Reference"
description: "The openspec CLI provides terminal commands for project setup, validation, status inspection, and management."
---

The `openspec` CLI complements AI slash commands (like `/opsx:propose`) with terminal commands for every stage of spec-driven development — from initial setup through archival.

## Command categories

| Category | Commands | Purpose |
|----------|----------|---------|
| **Setup** | `init`, `update` | Initialize and update OpenSpec in your project |
| **Browsing** | `list`, `view`, `show` | Explore changes and specs |
| **Validation** | `validate` | Check changes and specs for issues |
| **Lifecycle** | `archive` | Finalize completed changes |
| **Workflow** | `status`, `instructions`, `templates`, `schemas` | Artifact-driven workflow support |
| **Schemas** | `schema init`, `schema fork`, `schema validate`, `schema which` | Create and manage custom workflows |
| **Config** | `config` | View and modify settings |
| **Utility** | `feedback`, `completion` | Feedback and shell integration |

## Human vs agent commands

Most CLI commands are designed for human use in a terminal. Some commands also support agent and script use via JSON output.

### Human-only commands

These commands are interactive and designed for terminal use:

| Command | Purpose |
|---------|---------|
| `openspec init` | Initialize project (interactive prompts) |
| `openspec view` | Interactive dashboard |
| `openspec config edit` | Open config in editor |
| `openspec feedback` | Submit feedback via GitHub |
| `openspec completion install` | Install shell completions |

### Agent-compatible commands

These commands support `--json` output for programmatic use by AI agents and scripts:

| Command | Human use | Agent use |
|---------|-----------|----------|
| `openspec list` | Browse changes/specs | `--json` for structured data |
| `openspec show <item>` | Read content | `--json` for parsing |
| `openspec validate` | Check for issues | `--all --json` for bulk validation |
| `openspec status` | See artifact progress | `--json` for structured status |
| `openspec instructions` | Get next steps | `--json` for agent instructions |
| `openspec templates` | Find template paths | `--json` for path resolution |
| `openspec schemas` | List available schemas | `--json` for schema discovery |

## Global options

These options work with all commands:

| Option | Description |
|--------|-------------|
| `--version`, `-V` | Show version number |
| `--no-color` | Disable color output |
| `--help`, `-h` | Display help for command |

## Environment variables

| Variable | Description |
|----------|-------------|
| `OPENSPEC_CONCURRENCY` | Default concurrency for bulk validation (default: `6`) |
| `EDITOR` or `VISUAL` | Editor used by `openspec config edit` |
| `NO_COLOR` | Disable color output when set |

## Exit codes

| Code | Meaning |
|------|---------|
| `0` | Success |
| `1` | Error (validation failure, missing files, etc.) |

## Command reference

<CardGroup cols={2}>
  <Card title="init" icon="rocket" href="/reference/cli/init">
    Initialize OpenSpec in your project and configure AI tool integrations.
  </Card>
  <Card title="update" icon="arrows-rotate" href="/reference/cli/update">
    Regenerate AI tool configuration files after upgrading the CLI.
  </Card>
  <Card title="list" icon="list" href="/reference/cli/list">
    List active changes or specs in your project.
  </Card>
  <Card title="show" icon="eye" href="/reference/cli/show">
    Display details of a specific change or spec.
  </Card>
  <Card title="validate" icon="circle-check" href="/reference/cli/validate">
    Validate changes and specs for structural issues.
  </Card>
  <Card title="archive" icon="box-archive" href="/reference/cli/archive">
    Archive a completed change and merge delta specs into main specs.
  </Card>
  <Card title="status" icon="chart-bar" href="/reference/cli/status">
    Display artifact completion status for a change.
  </Card>
  <Card title="schema" icon="diagram-project" href="/reference/cli/schema">
    Create and manage custom workflow schemas.
  </Card>
  <Card title="config" icon="gear" href="/reference/cli/config">
    View and modify global OpenSpec configuration.
  </Card>
</CardGroup>
